{
  "name": "@app-upgrade/manager",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "deploy": "yarn --cwd build-scripts deploy",
    "start": "yarn --cwd app-upgrade-client start",
    "watch:common": "yarn --cwd common watch",
    "watch:functions": "yarn --cwd functions watch",
    "spellcheck": "cspell \"**/*\""
  },
  "workspaces": [
    "app-upgrade-client",
    "common",
    "functions",
    "scripts",
    "build-scripts"
  ],
  "devDependencies": {
    "cspell": "^5.2.4"
  }
}
